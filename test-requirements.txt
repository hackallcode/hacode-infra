# Python deps for running molecule scenarios locally and in CI.
# Upper bound is dictated by prometheus.prometheus 0.29.x which declares
# requires_ansible <=2.20.99. Bump together once that collection ships a
# release with broader compatibility.
ansible-core>=2.16,<2.21
ansible-lint>=24.2.0
yamllint>=1.35.0
molecule>=24.2.0
molecule-plugins[docker]>=23.5.0
docker>=7.0.0
pytest>=8.0.0
pytest-testinfra>=10.0.0
