- name: "Probe dnf Python bindings on target"
  tags: ["package"]
  ansible.builtin.command:
    cmd: "{{ ansible_python_interpreter | default('/usr/bin/python3') }} -c 'import dnf'"
  changed_when: false
  failed_when: false
  register: "_dnf_probe"
  when: "ansible_os_family == 'RedHat'"

- name: "Assert dnf Python bindings are available"
  tags: ["package"]
  ansible.builtin.assert:
    that: "_dnf_probe.rc == 0"
    fail_msg: >-
      Ansible's `dnf` module needs `python3-dnf` bindings for the active
      interpreter ('{{ ansible_python_interpreter | default('/usr/bin/python3') }}').
      Either install python3-dnf for that interpreter, or pin
      `ansible_python_interpreter` in inventory to a Python that already
      has the bindings (typically /usr/bin/python3 on RHEL-family).
  when: "ansible_os_family == 'RedHat'"

- name: "Import AlmaLinux GPG key"
  tags: ["package"]
  ansible.builtin.rpm_key:
    key: "{{ machine_alma_gpg_key_url }}"
    state: "present"
  when:
    - "ansible_distribution == 'AlmaLinux'"
    - "machine_alma_gpg_key_url | length > 0"

- name: "Install AlmaLinux repository"
  tags: ["package"]
  ansible.builtin.yum:
    name:
      - "almalinux-release"
    state: "present"
  when:
    - "ansible_distribution == 'AlmaLinux'"
    - "machine_install_alma_release | bool"

- name: "Drop custom yum repositories"
  tags: ["package"]
  check_mode: false
  ansible.builtin.copy:
    dest: "/etc/yum.repos.d/{{ item.name }}.repo"
    mode: "0644"
    content: |
      [{{ item.name }}]
      name={{ item.description | default(item.name) }}
      baseurl={{ item.baseurl }}
      enabled={{ item.enabled | default(1) }}
      gpgcheck={{ item.gpgcheck | default(0) }}
  loop: "{{ machine_yum_repos }}"
  loop_control:
    label: "{{ item.name }}"
  when:
    - "ansible_os_family == 'RedHat'"
    - "machine_yum_repos | length > 0"

- name: "Install EPEL repo"
  tags: ["package"]
  check_mode: false
  ansible.builtin.yum:
    name:
      - "epel-release"
    state: "present"
  when:
    - "ansible_os_family == 'RedHat'"
    - "machine_install_epel | bool"

- name: "Update apt cache (Debian/Ubuntu)"
  tags: ["package"]
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600
  when: "ansible_os_family == 'Debian'"

- name: "Check for elrepo .repo file"
  tags: ["package"]
  ansible.builtin.stat:
    path: "/etc/yum.repos.d/elrepo.repo"
    get_checksum: false
    get_attributes: false
    get_mime: false
  register: "_elrepo_stat"
  when:
    - "ansible_os_family == 'RedHat'"
    - "machine_nvidia_enabled | default(false) | bool"

- name: "Pin NVIDIA stack in elrepo"
  tags: ["package"]
  community.general.ini_file:
    path: "/etc/yum.repos.d/elrepo.repo"
    section: "elrepo"
    option: "excludepkgs"
    value: "kmod-nvidia* nvidia-*"
    mode: "0644"
  when:
    - "ansible_os_family == 'RedHat'"
    - "machine_nvidia_enabled | default(false) | bool"
    - "_elrepo_stat.stat.exists | default(false)"

- name: "Upgrade all packages"  # noqa: package-latest
  tags: ["package"]
  ansible.builtin.package:
    name:
      - "*"
    state: "latest"
  when: "machine_upgrade_all_packages | bool"

- name: "Install some new packages"
  tags: ["package"]
  ansible.builtin.package:
    name: "{{ machine_base_packages }}"
    state: "present"
  when: "machine_base_packages | length > 0"

- name: "Install RedHat-family base extras (firewalld, bind-utils)"
  tags: ["package"]
  ansible.builtin.package:
    name: "{{ machine_base_packages_redhat }}"
    state: "present"
  when:
    - "ansible_os_family == 'RedHat'"
    - "machine_base_packages_redhat | length > 0"

- name: "Install Debian-family base extras (firewalld, dnsutils)"
  tags: ["package"]
  ansible.builtin.package:
    name: "{{ machine_base_packages_debian }}"
    state: "present"
  when:
    - "ansible_os_family == 'Debian'"
    - "machine_base_packages_debian | length > 0"

- name: "Install RedHat-family extras"
  tags: ["package"]
  ansible.builtin.package:
    name:
      - "util-linux-user"
      - "yum-utils"
      - "python3"
      - "python3-pip"
      - "python3-devel"
      - "@Development Tools"
    state: "present"
  when: "ansible_os_family == 'RedHat'"

- name: "Install some Raspberry packages"
  tags: ["package"]
  ansible.builtin.package:
    name:
      - "wakeonlan"
      - "watchdog"
    state: "present"
  when: "is_raspberry_os | default(false)"

- name: "Install extra user-defined packages"
  tags: ["package"]
  ansible.builtin.package:
    name: "{{ machine_extra_packages }}"
    state: "present"
  when: "machine_extra_packages | length > 0"

- name: "Reconcile /etc/pip.conf [global] with machine_pip_* knobs"
  tags: ["package", "pip"]
  community.general.ini_file:
    path: "/etc/pip.conf"
    section: "global"
    option: "{{ item.k }}"
    value: "{{ item.v }}"
    state: "{{ 'present' if item.v | length > 0 else 'absent' }}"
    mode: "0644"
    create: true
  loop:
    - {k: "index-url", v: "{{ machine_pip_index_url }}"}
    - {k: "extra-index-url", v: "{{ machine_pip_extra_index_url }}"}
    - {k: "trusted-host", v: "{{ machine_pip_trusted_host }}"}
  loop_control:
    label: "{{ item.k }}"
  when: "machine_pip_index_url | length > 0"

# PEP 668 (Ubuntu 24.04+, Debian 12+, RHEL 10) drops an EXTERNALLY-MANAGED
# marker next to the stdlib and pip refuses system-wide installs without
# `--break-system-packages`. Older pip (Ubuntu 22.04) doesn't understand the
# flag and errors on it, so detect via the marker and pass the flag only
# where it's actually needed.
- name: "Detect Python PEP 668 externally-managed marker"
  tags: ["package", "pip"]
  ansible.builtin.find:
    paths: ["/usr/lib"]
    patterns: "EXTERNALLY-MANAGED"
    recurse: true
    depth: 4
  register: "_machine_pep668"
  changed_when: false
  when: "machine_pip_packages | length > 0"

- name: "Install pip packages (system-wide, via the configured index)"
  tags: ["package", "pip"]
  ansible.builtin.pip:
    name: "{{ machine_pip_packages }}"
    state: "present"
    extra_args: "{{ '--break-system-packages' if (_machine_pep668.matched | default(0) > 0) else omit }}"
  when: "machine_pip_packages | length > 0"
