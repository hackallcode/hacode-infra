- name: "Prepare: refresh apt cache on Debian family"
  hosts: "all"
  become: true
  gather_facts: true

  tasks:
    - name: "Refresh apt cache"
      ansible.builtin.apt:
        update_cache: true
        cache_valid_time: 600
      when: "ansible_os_family == 'Debian'"
